services:
  - type: web
    name: tandau-messenger
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python web_messenger.py
    envVars:
      - key: SECRET_KEY
        generateValue: true
