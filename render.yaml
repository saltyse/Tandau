services:
  - type: web
    name: tandau-messenger
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -k eventlet -w 1 "web_messenger:create_app()"
    envVars:
      PYTHON_VERSION: 3.13
